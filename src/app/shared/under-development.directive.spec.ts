import { UnderDevelopmentDirective } from './under-development.directive';

describe('UnderDevelopmentDirective', () => {
  it('should create an instance', () => {
    const directive = new UnderDevelopmentDirective();
    expect(directive).toBeTruthy();
  });
});
