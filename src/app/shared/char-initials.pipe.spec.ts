import { CharInitialsPipe } from './char-initials.pipe';

describe('CharInitialsPipe', () => {
  it('create an instance', () => {
    const pipe = new CharInitialsPipe();
    expect(pipe).toBeTruthy();
  });
});
